---
description: Sync local changes to GitHub
---

To sync your latest changes (like the new logo and offline fixes) to GitHub, run the following commands:

```bash
# 1. Add all changes
git add .

# 2. Commit the changes
git commit -m "Update app name, logo, and offline capabilities"

# 3. Push to GitHub
git push
```

// turbo-all
